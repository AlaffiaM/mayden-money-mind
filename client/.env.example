# API base URL. "/api" works for both local dev (Vite proxy -> :5000)
# and production (Vercel rewrite -> Render). Override with a full URL to
# bypass the rewrite, e.g. VITE_API_URL=https://mayden-money-mind.onrender.com/api
VITE_API_URL=/api
